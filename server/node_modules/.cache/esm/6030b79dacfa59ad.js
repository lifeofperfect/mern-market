let express;_acf‍.w("express",[["default",["express"],function(v){express=v}]]);

const router = express.Router();

router.get("/:message", (req, res)=> {
    res.status(200).send(`response message is: ${req.params.message}`);
});

module.exports = router;